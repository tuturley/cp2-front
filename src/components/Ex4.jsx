// Ex4 — Card responsivo

// Crie um card com título, texto e botão. O card deve ter largura fixa em telas grandes (400px) e ocupar `100%` em dispositivos móveis.

export default function Ex4() {
  return (
    <div>
        <h1 className="text-2xl font-bold mb-4">Ex4 - Card Responsivo</h1>
      {/* Resolva aqui */}
    </div>
  );
}